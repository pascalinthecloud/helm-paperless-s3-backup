apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-paperless-s3-backup.fullname" . }}
  labels:
    {{- include "helm-paperless-s3-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-paperless-s3-backup.selectorLabels" . | nindent 4 }}
