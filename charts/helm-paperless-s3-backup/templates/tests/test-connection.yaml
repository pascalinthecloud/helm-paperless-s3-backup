apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-paperless-s3-backup.fullname" . }}-test-connection"
  labels:
    {{- include "helm-paperless-s3-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-paperless-s3-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
